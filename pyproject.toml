
[tool.pytest.ini_options]
minversion = "6.0"
addopts =  "--junitxml=out/tests/pytest/test-results.xml --cov=src --cov-report=xml:out/tests/pytest/coverage.xml"
testpaths = [ "test" ]
filterwarnings = [
    "error",
    "ignore::DeprecationWarning"
]